import * as React from "react";

import { Card, Text } from "../../../lib";

const WakeUpCard = () => {
  return (
    <Card>
      <Text>WakeUpCard</Text>
    </Card>
  );
};

export default WakeUpCard;
