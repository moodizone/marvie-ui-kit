import * as React from 'react';
import Layout from "../../layout";
import {Text} from "../../lib";

const GiftCard = () => {
  return (
    <Layout>
      <Text>GiftCard</Text>
    </Layout>
  );
};

export default GiftCard;
